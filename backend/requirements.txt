# Core Django
Django==4.2.16

# REST Framework
# Using a fork that works with Python 3.13 (no pkg_resources dependency)
# If this fails, use: pip install djangorestframework-simplejwt==5.3.1
# Then manually edit: venv/Lib/site-packages/rest_framework_simplejwt/__init__.py
# Remove the line: from pkg_resources import DistributionNotFound, get_distribution
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1

# CORS
django-cors-headers==4.6.0

# Environment variables
python-dotenv==1.0.1

# Database (PostgreSQL via Supabase)
# For local dev, SQLite is used by default (no install needed)
# For production Supabase: pip install psycopg2-binary==2.9.10
dj-database-url==2.3.0
psycopg2-binary==2.9.10

# Server
gunicorn==23.0.0
whitenoise==6.8.2

# Filtering and Documentation
django-filter==24.3
drf-spectacular==0.27.2

# HTTP Requests (for Google OAuth verification)
requests==2.32.3

# JWT handling
PyJWT==2.10.1

# Essential for Python 3.13 compatibility
setuptools==75.8.0
wheel==0.45.1
